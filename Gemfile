# frozen_string_literal: true

source "https://rubygems.org"

# gem "rails"

# For running the site locally
gem "jekyll", ">= 4.0"
# Ensure a compatible HTTP server for `jekyll serve` on some Ruby versions
gem "webrick", ">= 1.7"

# Theme and plugins used by this site
gem "minima"
gem "jekyll-feed"
gem "jekyll-seo-tag"
gem 'wdm', '>= 0.1.0'